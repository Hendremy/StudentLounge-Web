export default class ValidatedTutoring{

    constructor({id, tutor, tutored, lesson}){
        this.id = id;
        this.tutor = tutor;
        this.tutored = tutored;
        this.lesson = lesson;
    }
}